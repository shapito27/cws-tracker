{
  "name": "cws-tracker-proxy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.9",
    "@cloudflare/workers-types": "^4.20241230.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.62.0"
  }
}
